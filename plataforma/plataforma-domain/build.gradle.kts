// plataforma-domain — shared kernel PURO (dinheiro decimal, tenancy, tipos-base).
// Invariante de fronteira (ADR-0002): domínio NÃO depende de application, infra
// nem de framework (Spring/JPA). Manter este bloco sem essas dependências.
dependencies {
    // intencionalmente vazio
}
