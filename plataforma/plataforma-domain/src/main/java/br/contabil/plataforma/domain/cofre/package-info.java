/**
 * Port <b>Cofre de segredos</b> (doc 11 §Cofre) — contrato estável do cofre
 * único (privilégio mínimo, referência lógica, segredo redigido em log). Camada PURA:
 * sem Spring/JPA. O adapter F0 é passthrough de ambiente/secret file (ADR-0024).
 */
package br.contabil.plataforma.domain.cofre;
