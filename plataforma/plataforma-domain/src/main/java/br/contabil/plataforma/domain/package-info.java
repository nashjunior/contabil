/**
 * Shared kernel do sistema contábil — tipos-base do domínio (dinheiro decimal,
 * tenancy) reutilizados por todos os contextos. Camada PURA: sem Spring/JPA.
 */
package br.contabil.plataforma.domain;
