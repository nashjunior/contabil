// razao-domain — livro razão (partidas dobradas, append-only, Σdébito = Σcrédito).
// Depende apenas do shared kernel (plataforma-domain). Sem framework.
dependencies {
    api(project(":plataforma:plataforma-domain"))
}
