# Design system SIAFIC — tokens e componentes-núcleo (F1)

[← Arquitetura técnica](./README.md) · [Fluxo do operador + contrato de API](./fluxo-execucao-operador-contrato-api.md) · [ADR-0026](./adr/0026-design-system-figma-decisoes-estruturais.md) · [04-lgpd](../transversais/04-lgpd.md) · [05-acessibilidade](../transversais/05-acessibilidade.md)

> Design-system-first (RAZ-100): tokens (cor/tipografia/espaçamento/elevação) + 10 componentes-núcleo em Figma, **sem telas** — a biblioteca contra a qual as telas do RAZ-79 nascem depois. Decisões estruturais (terminologia, escopo, tenant do Figma) estão no [ADR-0026](./adr/0026-design-system-figma-decisoes-estruturais.md); este documento é o mapeamento token→uso e o inventário da biblioteca.

**Arquivo Figma:** [SIAFIC — Design System (F1 execução)](https://www.figma.com/design/ObQu8oMQ0cEGbONMXgpuLU) (`fileKey ObQu8oMQ0cEGbONMXgpuLU`, plano "internal projects"). 20+ páginas: `Cover`, `Foundations` (cor/tipografia/espaçamento/elevação), e uma página por componente — inclui `10 Alerta / Mensagem Inline (RAZ-144)` (renomeada de `10 Aviso de Gap (API)`; adicionada durante RAZ-102, generalizada durante RAZ-144 — ver linha 11 da tabela abaixo), `11 Tabela — Balancete (RAZ-112)` (consultas/relatórios; ver linha 12), `13 Tabela — Dotações (RAZ-140)` (ver linha 13), `13 Picker de Conta PCASP (RAZ-142)` (ver linha 14), `15 Select Customizado — Estados (RAZ-145)` (ver linha 15), `13 Linha do Tempo — Trilha de Auditoria (RAZ-143)` (ver linha 16) e `17 Visualizador de Documento (RAZ-141)` (ver linha 18). O arquivo também contém páginas de telas fora do escopo desta biblioteca: `10 Telas core F1 (RAZ-102)` (operador — empenho/liquidação/pagamento), `12 Telas de consultas e relatórios (RAZ-112)` (saldo por conta/balancete/execução orçamentária — RAZ-142 acrescenta "Tela 4 — Catálogo de Contas PCASP" nesta mesma página), `14 Telas — Dotação / Crédito Orçamentário (RAZ-140)` (upstream do empenho), `16 Tela — Trilha de Liquidação (RAZ-143)` (auditoria de uma liquidação, somente leitura) e `18 Telas de assinatura de empenho (RAZ-141)` (§5.5/§6.10 do fluxo — preview, redirecionamento gov.br, trilha e os 5 estados da assinatura do empenho).
>
> **Nota de numeração (RAZ-142, ampliada em RAZ-143/RAZ-141):** o Figma é editado concorrentemente por várias issues no mesmo heartbeat (RAZ-140/141/142/143/144/145 todas criaram página nova no mesmo dia); três páginas colidiram no prefixo `13` (`Tabela — Dotações`/RAZ-140, `Picker de Conta PCASP`/RAZ-142, `Linha do Tempo — Trilha de Auditoria`/RAZ-143). Cada agente numerou pelo estado que via no início da própria sessão — não há dono único da sequência. **RAZ-143 foi além da colisão cosmética de número**: uma chamada `figma.createPage()` para a *tela* (não o componente) recebeu o mesmo **id de nó** de uma página criada quase simultaneamente pela RAZ-140, e conteúdo dos dois agentes chegou a ser empilhado no mesmo nó físico antes de ser detectado e corrigido (tela realocada para página nova, nada da RAZ-140 foi tocado) — detalhe em `figma-use-figma-gotchas` (memória do agente). **RAZ-141 acertou a mesma colisão de nome** (páginas criadas como `13 Visualizador de Documento`/`14 Telas de assinatura de empenho`, prefixos já tomados) mas sem colisão de **id de nó**; corrigido só renumeando para `17`/`18` (livres no momento da checagem) após confirmar que o conteúdo próprio permanecia intacto — `id` de página é estável a rename. **Merge doc (RAZ-152):** ao mesclar a doc de RAZ-141 em `master`, as linhas de tabela `Badge Assinatura`/`Visualizador de Documento` foram renumeradas de `16`/`17` (numeração da branch de RAZ-141) para `17`/`18`, porque RAZ-143 já ocupara a linha `16` (`Linha do Tempo`) em `master` — mesma disciplina de renumerar o retardatário, sem tocar a linha de outra issue. Registrado aqui em vez de renumerar por conta própria a página de outra issue; revisitar numa faxina de numeração só quando as issues concorrentes já tiverem convergido.
>
> **Nota sobre perda de commit sob concorrência (RAZ-143):** a primeira tentativa de registrar esta seção (commit local, branch `raz-128-bff-login-oidc-govbr`) desapareceu do histórico entre uma chamada de ferramenta e a seguinte — outro agente avançou a mesma branch (RAZ-140/142/128) a partir de um ponto anterior ao meu commit, sem ele como ancestral. Mesmo padrão já documentado no ADR-0037/RAZ-133 (índice stale + commit amplo derruba trabalho concorrente sem aviso); reaplicado aqui direto contra o `HEAD` atual, sem tentar recuperar o commit perdido (reescrever histórico compartilhado é mais arriscado que reaplicar a mudança).
>
> **Nota sobre a discrepância doc↔Figma apontada em RAZ-140 (resolvida em RAZ-144):** a nota anterior aqui relatava que, num `use_figma` rodado durante RAZ-140, a página `49:2` ainda aparecia como `10 Aviso de Gap (API)` com só 2 variantes — uma leitura feita **antes** da generalização de RAZ-144 ter sido de fato aplicada no arquivo (as duas issues rodaram em paralelo no mesmo file). Reverificado ao vivo (`use_figma`, 2026-07-26, após a aplicação): a página `49:2` já é `10 Alerta / Mensagem Inline (RAZ-144)`, o componente (`49:6`) já é `Alerta / Mensagem Inline (Alert)` com as 4 variantes (`Nível=Crítico|Atenção|Informativo|Sucesso`) — não é mais um caso de "done mas não aplicado". As telas de RAZ-140 que instanciaram o componente com o nome/2-variantes antigos continuam válidas (mesmo componente, `mainComponent` não muda de identidade só por rename/novas variantes-irmãs).

---

## 1. Tokens

### 1.1 Primitivos (coleção `Primitives`, 1 modo `Value`, `scopes: []`)

Sóbrio/institucional: `neutral` (12 passos, cinza-azulado), `brand` (9 passos, azul institucional `#2F6CAE`), `success`/`warning`/`danger` (7 passos cada). Ocultos de todo picker — só a camada semântica é exposta a quem monta tela.

**Export DTCG (RAZ-125):** `frontend/packages/design-system/tokens/color.tokens.json` (versionado no repo) carrega os passos de `Primitives` alcançáveis pela camada semântica (§1.2) com hex real, confirmado via Figma REST API — ver ADR-0031 (atualização RAZ-125) e `frontend/packages/design-system/tokens/README.md` para o método e o resíduo pendente (passos sem alias semântico, que exigem a Variables API ou export via Tokens Studio).

### 1.2 Semânticos (coleção `Color`, 1 modo `Value`)

Sem modo claro/escuro: back-office não pede tema (nenhum doc do produto exige dark mode); um único modo evita escopo não pedido. Se o citizen portal (fora do F1) vier a adotar o **Design System gov.br** ([05-acessibilidade](../transversais/05-acessibilidade.md)), esta camada semântica pode ganhar um segundo modo depois — decisão futura, não deste issue.

| Token | Alias (primitivo) | Uso |
| --- | --- | --- |
| `bg/canvas` | `neutral/50` | Fundo de página/tela |
| `bg/surface` | `neutral/0` | Fundo de card, input, modal, linha de tabela |
| `bg/inset` | `neutral/100` | Cabeçalho de tabela, barra de rodapé, campo desabilitado (base) |
| `bg/disabled` | `neutral/100` | Fundo de campo/estado desabilitado |
| `bg/overlay-scrim` | `neutral/900` | Scrim atrás de modal (uso com opacidade reduzida) |
| `text/primary` | `neutral/900` | Texto principal, valor monetário, título |
| `text/secondary` | `neutral/600` | Labels, legendas, texto de apoio |
| `text/tertiary` | `neutral/500` | Placeholder, hints, captions de documentação |
| `text/disabled` | `neutral/400` | Texto em campo/estado desabilitado |
| `text/on-brand` | `neutral/0` | Texto sobre fundo `brand/*` ou botão primário |
| `text/link` | `brand/600` | Link, item selecionado em menu (Seletor de Ente) |
| `border/default` | `neutral/300` | Borda padrão de input, card, linha de tabela |
| `border/strong` | `neutral/400` | Borda de checkbox/botão secundário, contraste maior |
| `border/focus` | `brand/500` | Anel de foco (teclado) — WCAG 2.2 foco visível |
| `brand/default` | `brand/500` | Botão primário, item ativo, indicador de seleção |
| `brand/hover` | `brand/600` | Estado hover de elemento de marca |
| `brand/pressed` | `brand/700` | Estado pressed de elemento de marca |
| `brand/subtle-bg` | `brand/50` | Fundo de linha selecionada (tabela), item ativo no seletor de ente |
| `state/neutral-{bg,fg,border}` | `neutral/{100,700,300}` | Badge de estágio **Empenhado** |
| `state/info-{bg,fg,border}` | `brand/{50,700,200}` | Badge de estágio **Liquidado** |
| `state/success-{bg,fg,border}` | `success/{50,700,300}` | Badge de estágio **Pago** · badge de aprovação **Aprovado** · banner de sucesso (Resumo Fail-soft) |
| `state/warning-{bg,fg,border}` | `warning/{50,700,300}` | Badge de aprovação **Pendente** · aviso anti-auto-aprovação (Gate 4-eyes) |
| `state/danger-{bg,fg,border}` | `danger/{50,700,300}` | Badge de aprovação **Devolvida** · erro de campo (`saldo_insuficiente`) · lista de rejeitados (Resumo Fail-soft) |
| `pii/mask-bg`, `pii/mask-fg` | `neutral/{100,700}` | Chip de CPF mascarado (estado default) |

**Contraste (WCAG AA, back-office — [05-acessibilidade](../transversais/05-acessibilidade.md)):** todos os pares `*-fg` sobre `*-bg` usados nos badges/chips foram checados pela fórmula de luminância relativa (WCAG 2.x) e ficam ≥ 7:1; `text/on-brand` (branco) sobre `brand/default` fica em ~5.4:1 — passa AA (≥4.5:1) mesmo em texto pequeno. Validação é manual nesta v1 (não há gate automatizado de contraste no Figma); o gate de CI com axe-core é item do F1 de acessibilidade ([05-acessibilidade](../transversais/05-acessibilidade.md)), não deste issue.

### 1.3 Espaçamento e raio (coleções `Spacing`, `Radius`, 1 modo `Value`)

`spacing/2xs..3xl` = 2/4/8/12/16/24/32/48px (`scope: GAP`). `radius/none..full` = 0/4/8/12/999 (`scope: CORNER_RADIUS`).

### 1.4 Tipografia (text styles, família Inter)

| Estilo | Tamanho/altura | Uso |
| --- | --- | --- |
| `Heading/H1` | 24/32 Semi Bold | Título de tela |
| `Heading/H2` | 20/28 Semi Bold | Título de seção (ex.: fila "Pagamentos pendentes") |
| `Heading/H3` | 16/24 Semi Bold | Título de card/modal (Gate de Aprovação, Formulário PCASP) |
| `Body/Default` | 14/20 Regular | Corpo padrão |
| `Body/Strong` | 14/20 Semi Bold | Ênfase em corpo (contagem de seleção, banner de sucesso) |
| `Body/Small` | 12/16 Regular | Texto auxiliar |
| `Label/Default` | 12/16 Medium, tracking 0.4 | Badges, cabeçalho de tabela, label de campo (versaliza no uso) |
| `Money/Default` | 14/20 Regular, tabular, alinhado à direita | Valor monetário em linha/campo |
| `Money/Emphasis` | 16/24 Semi Bold, tabular | Total/destaque (resumo de lote) |

### 1.5 Elevação (effect styles)

`Elevation/Raised` (linha/card) · `Elevation/Overlay` (dropdown do Seletor de Ente) · `Elevation/Modal` (Gate de Aprovação).

---

## 2. Componentes-núcleo (10, um por página)

| # | Componente | Variantes | Ancoragem |
| --- | --- | --- | --- |
| 1 | **Valor Monetário** | `Ênfase=Padrão\|Forte` | ADR-0006 (decimal, nunca float); tabular, alinhado à direita |
| 2 | **Campo de Valor** | `State=Default\|Focus\|Error\|Disabled` | Erro reflete `ErroContrato` do servidor (§6.1 do contrato) sem recalcular no cliente |
| 3 | **CPF Mascarado (PII)** | `Estado=Mascarado\|Integral` | 04-lgpd — máscara `***.456.***-**` é o default; `Integral` exige escopo elevado e é sempre auditado |
| 4 | **Seletor de Ente** | `State=Default\|Open\|Disabled` | ADR-0003 (RLS deny-by-default) — troca de tenant é ação explícita |
| 5 | **Badge Estágio** | `Estágio=Empenhado\|Liquidado\|Pago` | Read model derivado do saldo (§3 do fluxo) — nunca escrito direto |
| 6 | **Badge Aprovação** | `Aprovação=Pendente\|Aprovado\|Devolvida` | Estado forte (ADR-0023) — ver nota de terminologia no ADR-0026 |
| 7 | **Tabela — Seleção em Lote** | `Seleção=Nenhuma\|Parcial` | ADR-0022 — **só existe para pagamento**; linha com beneficiário PF usa o componente CPF Mascarado |
| 8 | **Formulário — Conta PCASP** | `Escriturável=Analítica\|Sintética` | `conta_pcasp` (razao-contabil-schema) — fora do fluxo de operador do F1, ferramental de administração do plano de contas |
| 9 | **Gate de Aprovação (4-eyes)** | `Decisão=Aprovar\|Devolver` | ADR-0023/RAZ-92 — `Devolver` exige motivo; aviso anti-auto-aprovação é só reforço de UX, a checagem real é no servidor |
| 10 | **Resumo Fail-soft** | `Estado=TudoOk\|ParcialComErros` | ADR-0013/ADR-0022 — materializa a resposta `207` de `POST /pagamentos:lote` |
| 11 | **Alerta / Mensagem Inline (Alert)** | `Nível=Crítico\|Atenção\|Informativo\|Sucesso` | Criado como **Aviso de Gap (API)** durante RAZ-102 (só `Crítico`/`Atenção`, para sinalizar lacuna de API na tela — busca em `search_design_system` não retornou nada equivalente). **Generalizado em RAZ-144**: auditoria achou "texto de erro" com estilo inline repetido ad hoc em 4 lugares fora deste componente — a nota anti-auto-aprovação do componente 9 `Gate de Aprovação (4-eyes)` (bakeada como `TEXT` solto em cada uma das 2 variantes `Decisão=Aprovar`/`Decisão=Devolver`) e o `success-banner` do componente 10 `Resumo Fail-soft` (bakeado como `FRAME` solto em cada uma das 2 variantes `Estado=TudoOk`/`Estado=ParcialComErros`). As 4 instâncias ad hoc foram substituídas por instâncias deste componente (`Nível=Atenção` para a nota "REGRA 9"; `Nível=Sucesso` para a confirmação de pagamentos efetivados) — texto do corpo (`Corpo`) passou de largura fixa (400px) para `FILL`, para caber em containers mais estreitos que os 428px nativos do componente sem clipar. Duas variantes novas: `Informativo` (azul, `state/info-*`, alias de `brand/{50,700,200}`) e `Sucesso` (verde, `state/success-*`). **A11y:** `Crítico`/`Atenção` mapeiam para `role="alert"` (região assertiva — erro/risco que exige atenção imediata); `Informativo`/`Sucesso` mapeiam para `role="status"` (região polite — confirmação/contexto, sem interromper o leitor de tela); nenhuma variante depende só de cor, o rótulo (`Título`) em versalete + o corpo carregam o significado. Documentação de uso/a11y como frame próprio na página do componente. Página própria `10 Alerta / Mensagem Inline (RAZ-144)` (renomeada de `10 Aviso de Gap (API)`), entre `09 Resumo Fail-soft` e as telas do RAZ-102 |
| 12 | **Tabela — Balancete** | (sem variantes — colunas fixas) | Adicionado durante RAZ-112 (consultas/relatórios): busca em `search_design_system` mostrou que "Table" na biblioteca "Simple Design System" já anexada é só um **ícone** (40×40), não um componente de dados — nada cobria uma tabela de relatório somente-leitura. Diferente do componente 7 (`Tabela — Seleção em Lote`, específico do lote de pagamento com checkbox), este espelha `BalanceteResponse`/`LinhaBalancete` (RAZ-101) campo a campo — Código/Descrição/Saldo anterior/Mov. Débito/Mov. Crédito/Saldo atual, mais rodapé com totais e o indicador "Confere" (Σdébito=Σcrédito, materializa `Balancete.confere()`), que reusa o componente **`Tag`** da "Simple Design System" (`Scheme=Positive|Danger`, `Removable=false`) em vez de um pill hand-rolled — achado corrigido a partir de feedback do board (RAZ-112: "olha primeiro componentes que podem ser reutilizáveis"; revisão de `search_design_system` por "tag"/"card"/"badge" depois de uma primeira entrega que tinha esse indicador e os cards de estatística hand-rolled). Mesma paleta/tipografia do componente 7 (variáveis de cor reaproveitadas diretamente) para as células/cabeçalho da tabela em si — só o indicador de status virou instância de biblioteca. Página própria `11 Tabela — Balancete (RAZ-112)` |
| 13 | **Tabela — Dotações** | (sem variantes — colunas fixas) | Adicionado durante RAZ-140 (dotação/crédito orçamentário, upstream do empenho): busca em `search_design_system` por "table"/"tag"/"tabs"/"card" confirmou de novo que "Table" da "Simple Design System" é só ícone (mesmo achado da linha 12) — nenhum componente de dados genérico cobre a listagem. Diferente dos componentes 7 e 12, espelha `Dotacao`+`SaldoDotacao` (execucao-domain) campo a campo — Classificação orçamentária/Fonte de recurso/Unidade gestora/Exercício/Autorizado/Empenhado(comprometido)/Disponível, mais uma coluna **"Bloqueado"** que fica sempre `— (n/d)`: `SaldoDotacao` só modela `valorAutorizado`/`valorComprometido`, o estágio `(Reserva)` opcional desenhado em `execucao-orcamentaria-despesa.md` (linha 18 da tabela de estágios) nunca ganhou `valorReservado`/`reservar()` no código — a coluna existe na tela porque o issue pediu "saldo bloqueado", mas o dado não existe, então a UI é honesta sobre o gap em vez de inventar um valor. Reusa o componente 1 (`Valor Monetário`) para as 3 colunas monetárias reais. Rodapé com nota de paginação por cursor (§6.1 do contrato) e a mesma nota do gap "Bloqueado". Página própria `13 Tabela — Dotações (RAZ-140)`. |
| — | **Card (Slot)** (Simple Design System, não é componente-núcleo próprio) | — | As telas de consultas (RAZ-112) usam `Card (Slot)` (container com slot vazio, biblioteca "Simple Design System") como wrapper dos cards de resultado (saldo consultado; totais de execução orçamentária) em vez de uma `FRAME` local com `cornerRadius`/`fill` hand-rolled — mesma correção de feedback acima. Não vira linha própria na tabela de componentes-núcleo por ser uma instância direta de biblioteca, sem variantes/campos definidos por este projeto. **Nota RAZ-140 (corrigida — auditoria de reuso, mesmo issue):** a entrega original tinha os 4 cards de saldo da tela "Detalhe da dotação" em `FRAME` local, com a justificativa registrada aqui de que a propriedade `SLOT` (`Slot#6062:63`) do `Card (Slot)` só aceitaria conteúdo via `instance.setProperties({...})`, não `appendChild` direto — **essa premissa estava errada**. Feedback do board (REUSO-FIRST, mesmo padrão já visto em RAZ-102/RAZ-110/RAZ-112) motivou reabrir o caso: o nó `SLOT` de uma instância aceita `appendChild` normalmente via `use_figma` (testado ao vivo — `slot.appendChild(labelText); slot.appendChild(valorMonetarioInstance)` funciona sem erro). Os 4 cards foram trocados para instâncias reais de `Card (Slot)`, reaproveitando os mesmos nós de label + `Valor Monetário` que já existiam (sem recriar texto/estilo). Efeito colateral encontrado e corrigido: `Card (Slot)` tem largura mínima guiada por conteúdo (~240px, pelo rótulo "EMPENHADO (COMPROMETIDO)" sem quebra) que não cabe 2-por-linha nos 432px de conteúdo do drawer (colunas fixas de 210px do desenho original); em vez de forçar a largura (mesmo "FILL não encolhe abaixo do hug" já documentado no RAZ-112), a `saldo-row` virou uma pilha vertical de 1 coluna com os 4 cards em `layoutSizingHorizontal=FILL` — mesmo padrão de uso já canônico na página 12 (RAZ-112). |
| — | **Tag** (Simple Design System) | `Scheme=Brand\|Neutral\|Positive\|Danger\|Warning` | Reusado na tela "Detalhe da dotação" (trilha de créditos adicionais) para o `tipo` do crédito (`SUPLEMENTAR`/`ESPECIAL`/`EXTRAORDINARIO` — enum `TipoCreditoAdicional`), mesma família já usada no indicador "Confere" do componente 12. |
| — | **Tab** (Simple Design System) | `State=Default\|Hover` × `Active=Off\|On` | Reusado na tela "Nova carga" para alternar Fixação/Crédito adicional — as duas listas independentes que `IngerirDotacoes.executar(fixacoes, creditos)` já aceita, cada uma cobrada por uma `Acao` RBAC distinta (`CRIAR` vs `ALTERAR`). |
| 15 | **Select Customizado — Painel (estado assíncrono)** | `Estado=Carregando\|Vazio\|Erro` | Adicionado durante RAZ-145 (design dos estados do futuro componente de código RAZ-122 — typeahead + filtro + múltiplo + async). Não é o mesmo caso do `Picker de Conta PCASP` (linha 14, RAZ-142): aquele é o combobox de busca **single-select**, sem estados de loading/vazio/erro assíncronos próprios (só `Vazio\|Selecionada\|Aberto`); RAZ-122 é o primitivo **genérico** de formulário (single **e** múltiplo, com `useAsyncOptions` — debounce/`AbortController`/cache/paginação), então precisa de estados que o picker não modela. Busca em `search_design_system` ("combobox", "autocomplete", "multi-select", "tag input", "spinner"/"loading", "listbox") não achou um componente pronto equivalente — reaproveitados os átomos existentes: `Select Field` (Simple Design System) já renderiza sozinho fechado/aberto/foco(anel `border/focus`)/erro/desabilitado só ligando a variant `Open`; o painel de estado assíncrono (loading/vazio/erro) é a única peça de UI genuinamente nova, usando os ícones `Loader`/`Inbox`/`Alert circle` (Simple Design System) recoloridos com as variáveis semânticas deste arquivo (`text/secondary`, `state/danger-fg`). Página própria `15 Select Customizado — Estados (RAZ-145)`. |
| — | **Select Customizado — Múltiplo (chips)** (sem variantes) | — | Segunda peça nova de RAZ-145: o modo `multiple` do RAZ-122 mostra os valores selecionados como chips removíveis dentro do próprio campo, algo que o `Select Field` (single-value) não suporta. Reusa a casca visual do `Select Field` (mesmo `radius/md`, `border/default`, padding) com instâncias do componente `Tag` (Simple Design System, `Scheme=Neutral, Variant=Secondary, Removable=true`) no lugar do texto de valor único — nenhum chip foi hand-rolled. Mesma página `15 Select Customizado — Estados (RAZ-145)`. |
| 14 | **Picker de Conta PCASP** | `Estado=Vazio\|Selecionada\|Aberto` | Adicionado durante RAZ-142 (catálogo/picker de conta PCASP): lê `CatalogoContasController`/`ConsultarContas`/`CatalogoContasPort` (RAZ-117/ADR-0030 §6) — endpoint `GET /razao/contas?busca=&cursor=&limit=` já implementado, sem lacuna de API a sinalizar aqui (ao contrário dos componentes 11-13). Busca em `search_design_system` por "combobox"/"autocomplete"/"search"/"tree"/"select field"/"list item"/"menu item"/"dialog"/"tag" não achou nenhum combobox-com-busca pronto na "Simple Design System" — a peça nova é só a composição: `Search` (campo de busca) + `Menu Item` (linha, `Has Icon`/`Has Shortcut` desligados) + `Tag` (`Scheme=Positive`→Analítica selecionável, `Scheme=Neutral`→Sintética não-selecionável, mesma regra do componente 8). `Estado=Vazio`/`Selecionada` é o campo fechado (mesma linguagem visual de rótulo do antigo `Select Field` ad hoc que ele substitui — ver Tela 1 abaixo); `Estado=Aberto` é o painel dropdown expandido, reaproveitando a mesma composição visual (`Elevation/Overlay`) do componente 4 (`Seletor de Ente`, variante `State=Open`) em vez de um modal de tela cheia — segue o precedente já existente no arquivo de modelar o estado expandido como variante do mesmo component set, não como componente à parte. **Hierarquia derivada, não servida:** a API devolve lista PLANA ordenada por `código` (sem endpoint "filhos de X" — só `busca=` por prefixo de código OU `descricao ilike`); a indentação visual entre `Ativo circulante` (sintética) e `Caixa e equivalentes de caixa` (analítica) é calculada client-side a partir do nº de segmentos do código, documentado no frame de Documentation da página. Rodapé com botão "Carregar mais" (não paginação numerada — o contrato é cursor opaco, §6.1). Instanciado na Tela 1 — Saldo por conta PCASP (página 12, RAZ-112) no lugar do `Select Field` ad hoc que ali simulava a busca sem backend real, e na nova Tela 4 — Catálogo de Contas PCASP (mesma página 12), a tela de navegação/consulta do plano de contas que o issue também pediu. Não instanciado em Novo Empenho (página 10, RAZ-102): o contrato ratificado do empenho (§6.3) referencia `classificacaoOrcamentaria` via wizard, não um `contaId` de `conta_pcasp` direto — encaixar o picker ali seria desenhar contra um campo que a API não expõe hoje; o padrão fica pronto para qualquer formulário futuro que abra esse campo. Página própria `13 Picker de Conta PCASP (RAZ-142)` (colisão de numeração com a linha 13 — ver nota de numeração no topo do documento). |
| 16 | **Linha do Tempo — Evento de Trilha** | `Tipo=Empenho Registrado\|Liquidação Registrada\|Aprovação Decidida — Aprovada\|Aprovação Decidida — Devolvida` × `Último=false\|true` (8 variantes) | Adicionado durante RAZ-143 (trilha/histórico de auditoria de uma liquidação). Busca em `search_design_system` por "timeline"/"activity log"/"audit trail"/"stepper"/"vertical steps" confirmou que nem a "Simple Design System" nem o restante do arquivo têm um componente de linha do tempo — o precedente mais próximo era o bloco compacto `field / Trilha` hand-rolled dentro do componente 9 (`Gate de Aprovação`, RAZ-110), um resumo de 4 linhas fixas sem noção de lista cronológica de tamanho variável nem conector visual — insuficiente para "ver o histórico completo" (a issue pede reaproveitar "o padrão de tabela/linha do tempo já existente", mas o padrão existente é só esse resumo estático). Cada instância = 1 `EventoAuditoria` de `TrilhaLiquidacao` (`ConsultarTrilhaLiquidacao`, ADR-0029 §3), servido por `GET /api/v1/entes/{enteId}/execucao/liquidacoes/{id}/trilha` já implementado (RAZ-115/RAZ-121, confirmado em `master`) — sem gap de API nesta tela. Reaproveita o componente 5 (`Badge Estágio`/`Badge Aprovação`) para o estado atingido pelo evento e o componente 3 (`CPF Mascarado`) para o `ator` — o contrato já devolve o ator mascarado do servidor. Rail (dot + linha conectora) liga eventos consecutivos; `Último=true` remove a linha após o evento final. Linha "detalhes" espelha o mapa livre `detalhes: Map<String,String>` do evento (chave técnica traduzida: `empenhoId` → "empenho vinculado", `decisao` → "decisão") sem assumir quais chaves existirão além das já usadas pelo use case. **Achado de alinhamento corrigido nesta issue:** o bloco compacto `field / Trilha` (componente 9, ambas as variantes `Decisão=Aprovar`/`Decisão=Devolver`) mostrava um nome de autor fictício ("M. Costa") nas linhas "AUTOR EMPENHO"/"AUTOR LIQUIDAÇÃO" — o contrato real (`EventoAuditoria.ator`) só devolve CPF mascarado, sem nome; corrigido para `***.456.***-** · DD/MM/AAAA` nas 4 linhas, e adicionado um link "Ver trilha completa (histórico) →" apontando para a tela nova (`16 Tela — Trilha de Liquidação`). Página própria `13 Linha do Tempo — Trilha de Auditoria (RAZ-143)` (colisão de numeração — ver nota no topo do documento). |
| 17 | **Badge Assinatura** | `Situação=Pendente\|Assinado\|Rejeitada` | Adicionado durante RAZ-141 (assinatura eletrônica do empenho): 3ª dimensão de badge na página 05, ao lado de `Badge Estágio` (linha 5) e `Badge Aprovação` (linha 6) — eixo **ortogonal** aos outros dois, não uma variante deles. `Badge Estágio` mostra o pipeline empenho→liquidação→pagamento; `Badge Assinatura` mostra o **ciclo de vida do documento** da nota (`Empenho.status`, ADR-0027 (d)) — a mesma separação documento↔comprometimento contábil do ADR-0027 §R1 ("`PENDENTE_ASSINATURA` não significa crédito não comprometido"), agora também expressa como dois badges independentes em vez de forçar um único eixo a carregar dois significados. Reusa exatamente a paleta/estrutura (pílula, `cornerRadius 999`, par bg/borda/texto por variável semântica) de `Aprovação=Aprovado`/`Pendente`/`Devolvida` — `Assinado`→verde (mesmas variáveis de `Aprovado`), `Pendente`→âmbar (mesmas de `Aprovação=Pendente`), `Rejeitada`→vermelho (mesmas de `Devolvida`) — sem inventar uma 4ª paleta de estado. Renumerada de `16` no merge de RAZ-152 (RAZ-143 já ocupava a linha 16). |
| 18 | **Visualizador de Documento (PDF)** | `Estado=Pendente\|Assinado` | Adicionado durante RAZ-141: card de preview da nota de empenho (nome do arquivo, data de geração, thumbnail estilizado + chip "PDF", ações "Abrir pré-visualização"/"Baixar") reusando `Button`/`Button Danger` (Simple Design System) para as ações. Busca em `search_design_system` (biblioteca Simple Design System + arquivo local) por "document preview PDF viewer file attachment" não retornou nada — mesmo padrão de ausência já registrado para Linha do Tempo (linha 16) e Tabela — Balancete/Dotações (linhas 12-13): a biblioteca cobre primitivos de formulário/navegação, não visualização de documento. `Estado=Assinado` acrescenta uma linha "Assinado digitalmente — hash conferido (PAdES)" e troca o rótulo do botão de download; nenhuma das duas variantes renderiza o PDF de verdade no próprio Figma (é maquete estática, como qualquer tela) — mas o bridge que a torna real, **`GET .../empenhos/{id}/documento`** ([ADR-0039](./adr/0039-contrato-leitura-assinatura-empenho-preview-retorno-oauth.md), RAZ-157), **já está em `master`**. O aviso inline `Alerta/Mensagem Inline`/`Nível=Crítico` que sinalizava esse gap nas 3 telas da página 18 foi removido na reconciliação da RAZ-176 (2026-08-01), já que o endpoint deixou de faltar — ver nota de reconciliação no parágrafo de cobertura abaixo. Página própria `17 Visualizador de Documento (RAZ-141)` (a página Figma é `17`; a linha da tabela é `18` após o merge de RAZ-152). |

Cobertura (antes de RAZ-112): 91 variáveis (43 primitivas + 35 semânticas + 13 espaçamento/raio), 9 text styles, 3 effect styles, 11 component sets / 27 variantes. RAZ-112 acrescenta 1 componente (`Tabela — Balancete`, sem variantes) — inventário completo não recontado nesta revisão. RAZ-144 não acrescenta componente novo (generaliza o 11º, `Alerta / Mensagem Inline`, de 2 para 4 variantes, aplicado e reverificado ao vivo no Figma) e remove 4 duplicações ad hoc (componentes 9 e 10) — nenhum componente novo, nenhuma página nova além da renomeação da página 10. RAZ-140 acrescenta 1 componente-núcleo (`Tabela — Dotações`, sem variantes) + 2 páginas de telas (`13`, `14`) reusando `Tag`/`Tab` da "Simple Design System" pela primeira vez neste arquivo. RAZ-142 acrescenta 1 componente-núcleo (`Picker de Conta PCASP`, 3 variantes) + 1 tela nova (Tela 4, página 12) — inventário completo não recontado nesta revisão. RAZ-145 acrescenta 2 peças novas (`Select Customizado — Painel de estado assíncrono`, 3 variantes; `Select Customizado — Múltiplo/chips`, sem variantes) + 1 página (`15 Select Customizado — Estados`) — inventário completo não recontado nesta revisão. RAZ-143 acrescenta 1 componente-núcleo (`Linha do Tempo — Evento de Trilha`, 8 variantes) + 1 página de tela (`16 Tela — Trilha de Liquidação`, drill-down de auditoria somente-leitura) e corrige um desalinhamento contrato↔mock pré-existente no componente 9 — ver linha 16 da tabela acima e as notas de concorrência no topo deste documento. RAZ-141 acrescenta 2 componentes-núcleo (`Badge Assinatura`, 3 variantes, terceira dimensão da página 05, linha 17; `Visualizador de Documento (PDF)`, 2 variantes, página Figma `17`/linha 18) + 5 telas de assinatura do empenho (página `18`: pendente, confirmação modal, retorno do gov.br, assinado, rejeitada) — ver [§5.5/§6.10 do fluxo](./fluxo-execucao-operador-contrato-api.md#55-tela-nota-de-empenho--assinatura-raz-141-papel-ordenador) e [ADR-0039](./adr/0039-contrato-leitura-assinatura-empenho-preview-retorno-oauth.md) (Aceita, RAZ-152) para os 3 gaps de leitura que as telas sinalizavam inline (delegados a RAZ-156/157/158). Durante a construção, RAZ-141 reusou o componente 11 (`Alerta/Mensagem Inline`, já generalizado por RAZ-144) para a nota de comprometimento, a confirmação e o erro de certificado, em vez de reintroduzir 3 caixas hand-rolled.

**Reconciliação RAZ-176 (2026-08-01):** os 3 gaps de leitura da ADR-0039 já chegaram a `master` (RAZ-156 `GET /empenhos/{id}`, RAZ-157 `GET /empenhos/{id}/documento`, RAZ-158 callback `302`; conferido linha a linha no código do caminho autoritativo, não só pelo status `done` do board — disciplina da [ADR-0040](./adr/0040-verificacao-identidade-workspace-antes-de-done.md)). Varredura de texto na página 18 encontrou 3 pontos desatualizados, corrigidos ao vivo no Figma: (1) removido o aviso `Alerta/Mensagem Inline`/`Nível=Crítico` "GAP DE API — sem endpoint de preview/download" das 3 telas (pendente/assinado/rejeitada), já que o bridge RAZ-157 existe; (2) reescrito o aviso `Nível=Atenção` sobre o fluxo OAuth usar `HttpSession`/cookie — o fato em si continua verdadeiro (é a única exceção ao `Authorization: Bearer` do resto do contrato, por desenho do PKCE), mas a alegação "e não estão em `contrato-provisorio.yaml` ainda" ficou obsoleta assim que RAZ-156/157/158 documentaram os 5 endpoints do fluxo de assinatura no contrato (decisão 5 da ADR-0039); (3) corrigida a nota da tela "Retornando do gov.br" — descrevia a proposta original (mudar o `redirect_uri` registrado no gov.br para apontar ao SPA), que a ratificação da ADR-0039 (R1) rejeitou explicitamente a favor de manter o `redirect_uri` apontando para o backend e só trocar a resposta do callback (`204`/JSON → `302`), já implementado. **Achado adicional, mesma classe do desalinhamento já corrigido na linha 16 (`Linha do Tempo`):** o bloco `TRILHA DA ASSINATURA` (hand-rolled, específico de RAZ-141, distinto do componente 9 já corrigido) mostrava "05/07/2026 09:14 · M. Costa" na linha `EMPENHO REGISTRADO` — nome fictício sem nenhum campo correspondente em `EmpenhoRegistradoResponse`/`EmpenhoPorIdResponse` (nem mascarado: o contrato simplesmente não expõe quem registrou o empenho). Removido o fragmento do nome nas 3 telas, mantendo só a data/hora (mesmo padrão "sem dado inventado" já usado para "Bloqueado — (n/d)" em Dotações, linha 13).

---

[← Arquitetura técnica](./README.md) · [ADR-0026](./adr/0026-design-system-figma-decisoes-estruturais.md)
