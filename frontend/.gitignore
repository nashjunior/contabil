# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# RAZ-211 (E2E Playwright) — gerado por e2e/global-setup.ts a cada execução
# (JWTs assinados por execução, nunca comitar) + relatórios do Playwright.
e2e/.runtime.json
playwright-report
test-results

# Artefatos de build (ADR-0031/ADR-0033) — GERADOS, nunca editados a mao.
# `npm run tokens:build` / `npm run api:generate` regeneram antes de dev/build/test.
src/shared/tokens/theme.ts
src/shared/tokens/theme.css
src/shared/api/generated/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
